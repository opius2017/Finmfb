namespace FinTech.Core.Application.Common.Enums.Internal
{
    // Internal copy (moved to internal namespace to avoid conflict with domain enum)
    public enum AccountClassification
    {
        Asset,
        Liability,
        Equity,
        Income,
        Expense
    }
}
